import NavBar from "./NavBar"

const Footer = () => {
    return (
        <footer>
            <a href="#">Instagram</a>
            <a href="#">Facebook</a>
            <a href="#">Twitter</a>
            <a href="#">Whatsapp</a>
        </footer>
    )
}

export default Footer